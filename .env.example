# Supabase — projeto usado para armazenar os pedidos de diagnóstico do formulário de contato.
# Nunca use o prefixo NEXT_PUBLIC_ nestas duas: elas só são lidas no servidor (services/supabase/server.ts).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
